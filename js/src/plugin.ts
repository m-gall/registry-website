import {Workflow, PluginBase} from "cwl-svg";

export class SelectionPlugin extends PluginBase {
    registerWorkflow(workflow: Workflow): void {
        super.registerWorkflow(workflow);

        // Code here
    }
}
